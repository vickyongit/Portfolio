import React from "react";
import { CircleProps } from "../../types";
export declare const Circle: ({ radius, size, percentage, duration, color, startPosition, returnState, roundLineCap, animation, percentageAnimation, reverse, reverseDuration, loopCount, startDelay, reverseDelay, antiClockWise, margin, separator, styles, chartValue, }: CircleProps) => React.JSX.Element;
