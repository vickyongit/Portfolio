import React from "react";
import { LinearProgressBarProps } from "../../types";
import "./linear-styles.css";
declare const LinearProgressBar: ({ text, textStyle, percentageColor, percentage, showPercentage, color, trackColor, width, height, duration, percentagePosition, startDirection, animation, percentageAnimation, roundLineCap, }: LinearProgressBarProps) => React.JSX.Element;
export default LinearProgressBar;
